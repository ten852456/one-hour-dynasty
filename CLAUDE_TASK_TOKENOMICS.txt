# Task for Claude Code: Implement WUXIA Tokenomics

Please perform the following steps to bootstrap the blockchain layer for "One Hour Dynasty":

1. **Read Context**:
   - Read `d:/work/bonchain/monad_wuxia/CLAUDE_BLOCKCHAIN.md` (Architecture)
   - Read `d:/work/bonchain/monad_wuxia/docs/TOKENOMICS.md` (Token logic)

2. **Initialize Project**:
   - Create directory `packages/contracts`
   - Initialize a Hardhat project with TypeScript
   - Install `@openzeppelin/contracts`

3. **Implement Contracts**:
   - **`WuxiaToken.sol`**: ERC-20, Burnable, Ownable. 100M fixed supply.
   - **`ItemStore.sol`**:
     - Function `buyBoost(uint boostId)` -> Burns WUXIA.
     - Function `buySubscription(uint subId)` -> Sends WUXIA to Treasury.
     - Prices should be configurable by owner.

4. **Verify**:
   - Compile contracts to ensure no errors.
   - Create a basic test script to verify buying a boost burns tokens.

> **Note**: Focus on the Token and Store first. GameRegistry will be done in the next step.
